name: nosmai_agora_bridge_example
description: "Demonstrates how to use the nosmai_agora_bridge plugin."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ^3.10.0-127.0.dev

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  nosmai_agora_bridge:
    # When depending on this package from a real application you should use:
    #   nosmai_agora_bridge: ^x.y.z
    # See https://dart.dev/tools/pub/dependencies#version-constraints
    # The example app is bundled with the plugin so we use a path dependency on
    # the parent directory to use the current plugin's version.
    path: ../

  # Agora RTC — the example drives the engine directly; the bridge only feeds it
  # filtered frames. Version matches what tikshot ships so behaviour lines up.
  agora_rtc_engine: ^6.5.3

  # Camera permission for the preview.
  permission_handler: ^12.0.1

  # ── Nosmai camera SDK ─────────────────────────────────────────────────────
  # Published release by default, so a fresh clone builds with no local setup.
  #
  # To test against LOCAL SDK changes, comment out the hosted line and uncomment
  # the path override below, then `flutter pub get`. Swapping back is the same
  # two-line edit — no other file changes.
  nosmai_camera_sdk: ^3.0.6
  # nosmai_camera_sdk:
  #   path: ../../camsdk_v2

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # ── Filter assets (optional) ──────────────────────────────────────────
  # COMMENTED OUT ON PURPOSE. These directories are gitignored, so a fresh
  # clone does not have them — and Flutter FAILS THE BUILD on a declared asset
  # directory that does not exist, rather than skipping it.
  #
  # To try the example with filters:
  #   1. drop .nosmai packages into example/assets/nosmai_filters/<name>/
  #   2. uncomment the matching lines below (one per directory — Flutter does
  #      NOT recurse, so a single `assets/nosmai_filters/` line silently
  #      bundles nothing and getLocalFilters() returns an empty list)
  #   3. flutter pub get
  #
  # assets:
  #   - assets/nosmai_filters/data_glitch/
  #   - assets/nosmai_filters/fish_eye/
  #   - assets/nosmai_filters/gameboy/
  #   - assets/nosmai_filters/ghost_frame/
  #   - assets/nosmai_filters/neon_vibe/
  #   - assets/nosmai_filters/pixelated/
  #   - assets/nosmai_filters/reindeer_face/
  #   - assets/nosmai_filters/thermal_cam/
  #   - assets/nosmai_filters/wolf_boy/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
